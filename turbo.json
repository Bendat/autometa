{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["env", "CUCUMBER_FILTER_TAGS"],
  "pipeline": {
    "build": {
      "inputs": ["**/*.ts"],
      "outputs": ["dist/**"]
    },
    "build:docs": {},
    "integrate": {
      "dependsOn": ["^build"]
    },
    "clean": {
      "inputs": ["dist/**"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "lint": {
      "inputs": ["**/*.ts"]
    },
    "dev": {
      "cache": false
    }
  }
}
