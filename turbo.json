{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["env", "CUCUMBER_FILTER_TAGS"],
  "pipeline": {
    "build": {
      "inputs": ["**/*.ts"],
      "outputs": ["dist/**"]
    },
    "build:docs": {},
    "integrate": {
      "dependsOn": ["^build"]
    },
    "test": {},
    "prettify": {
      "inputs": ["**/*.ts"]
    },
    "lint": {
      "inputs": ["**/*.ts"]
    },
    "dev": {
      "cache": false
    }
  }
}
