import { pageObjectModelsPageComponentsPlaywright } from './page-components-playwright';

describe('pageObjectModelsPageComponentsPlaywright', () => {
  it('should work', () => {
    expect(pageObjectModelsPageComponentsPlaywright()).toEqual(
      'page-components-playwright'
    );
  });
});
