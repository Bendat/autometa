{
  "name": "@autometa/examples-runner-composition",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "autometa run --standalone",
    "test:watch": "autometa run --standalone --watch",
    "test:list": "autometa run --standalone --dry-run",
    "type-check": "tsc --project tsconfig.json --noEmit",
    "coverage": "node -e \"const fs=require('node:fs');fs.mkdirSync('coverage',{recursive:true});fs.writeFileSync('coverage/.keep','');\""
  },
  "dependencies": {
    "@autometa/core": "workspace:*",
    "@autometa/runner": "workspace:*",
    "@autometa/gherkin": "workspace:*"
  },
  "devDependencies": {
    "@autometa/cli": "workspace:*",
    "@types/node": "18.19.24",
    "typescript": "5.4.5"
  }
}
