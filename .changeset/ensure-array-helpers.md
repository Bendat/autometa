---
"@autometa/assertions": minor
---

Add fluent `ensure(...)` helpers for arrays/objects (`tap`, `map`, `each`, `pluck`, `prop`) and a `toBeTypeOf(...)` matcher. Improve `toBeInstanceOf(Array)` typing so chained array operations preserve element types.
