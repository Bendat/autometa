---
"@autometa/cli": patch
"@autometa/config": patch
"@autometa/core": patch
"@autometa/vitest-plugins": patch
---

Add configurable directory-based scoping for hoisted feature files (while keeping `@scope(...)` as an override), and improve runner filtering behaviour when using native Vitest.

